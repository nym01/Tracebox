# Base URL of the Tracebox HTTP API. The frontend calls `${VITE_TRACEBOX_API_URL}/run`.
# Copy this file to `.env` (or `.env.local`) and adjust if your API runs elsewhere.
VITE_TRACEBOX_API_URL=http://localhost:8080
